package Arrays;


public class ArrayBasics {
    // creating an array explicitly: 
    // first declare the type of data it holds:
    double[] prices; 
    // next, explicitly initialize the array to contain the data we want to store:
    prices = {13.15, 15.87, 14.22, 16.66};

    // can also declare on one line: 
    double[] prices = {13.15, 15.87, 14.22, 16/66};

    String[] clothingItems = {"Tank Top", "Beanie", "Funny Socks", "Corduroys"};
    
}

// an array holds a fixed number of values of ONE TYPE.  
// arrays hold doubles, ints, booleans or any other primitives
// arrays can also contain Strings as well as object references

